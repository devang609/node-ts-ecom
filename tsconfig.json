{
  "compilerOptions": {
    // File Layout
    "rootDir": "./src",          
    "outDir": "./dist",          

    // Environment Settings
    "module": "NodeNext",        // Node.js ESM-compatible module output.
    "target": "ESNext",          // Modern JavaScript (ESNext).
    "moduleResolution": "NodeNext",
    "allowImportingTsExtensions": true,
    "types": [],                 // No global types, manually include types like @types/node if needed.

    // For nodejs:
    // "lib": ["esnext"],         // Include ESNext features like async/await and modern syntax.
    // "types": ["node"],         // Uncomment for Node.js types.

    // Other Outputs
    "sourceMap": false,          
    "declaration": false,        
    "declarationMap": false,     
    "noEmit": true,
    "pretty": true,

    // Stricter Typechecking Options
    "noUncheckedIndexedAccess": true,  
    "exactOptionalPropertyTypes": true, 

    // Recommended Options
    "strict": true,               
    "jsx": "react-jsx",           
    "verbatimModuleSyntax": true,  
    "isolatedModules": true,      
    "noUncheckedSideEffectImports": true,  
    "moduleDetection": "force",   
    "skipLibCheck": true         
  },
  "include": ["src/**/*"],        
  "exclude": ["node_modules", "dist"]
}
